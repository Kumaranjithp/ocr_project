back = {'one': "one", 2: "two", 3: "three", 4: "four"}
print(back['one'])
print(len(back))
val = back.values() # values method to drag values as a list and saved in val variable
print(val)
print( 'two' in val)
# lists search algorithm is linear search.(time gets longer in direct proportion to the lrn of the list
# Dictionaries search algorithm is a hash table (hash func are so magical)


